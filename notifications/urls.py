from django.urls import path
from . import views  # Make sure views.py exists in the notifications app

urlpatterns = [
    # Example endpoint (you can add more later)
    # path('test/', views.TestView.as_view(), name='test'),
]
